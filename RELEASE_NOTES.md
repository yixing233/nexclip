# NexClip v20260905.01 - 覆盖安装占用修复与分平台更新通道

NexClip 是一套专为多端设备打造的现代化、轻量高效跨平台剪贴板同步与文本 / 图片消息互传系统。本次 **v20260905.01** 主要修两件事：Windows 端**覆盖安装时报「文件正由另一进程使用」而失败**的问题被彻底解决——根因是安装器自己继承了安装目录作为工作目录，占用者正是它本身；以及两端更新通道从"共用一个版本号"改成**按平台各判各的**，此后只发布单端不会再让另一端误报新版本。服务端本次无改动。

---

## 本次更新核心亮点 (Changelog)

### 1. Windows 桌面端

- **修复覆盖安装失败：`System.IO.IOException` 文件正由另一进程使用**。安装向导走到"正在部署应用程序文件"时把整个安装目录改名让位，这一步会失败，而且此前无论怎样退出 NexClip 都救不回来。五层改动：
  - **安装器启动后立刻脱离当前工作目录**，切到系统临时目录。进程的当前目录会持有一个不带 `FILE_SHARE_DELETE` 的目录句柄，该目录因此无法被改名；旧版桌面端用 `ShellExecute` 启动安装器时没有指定工作目录，安装器便继承了桌面端的当前目录（快捷方式把它设成了安装目录）——真正的占用者是安装器自己，所以"先退出 NexClip 再装"不管用。
  - **桌面端启动安装器时显式指定工作目录**，从源头断掉这条继承链。
  - **整目录改名带退避重试**（6 次，间隔 250ms 递增），应对杀进程后句柄释放、杀毒软件扫描慢一拍的情况。
  - **改名实在不成就退回逐文件覆盖**：exe / dll 这类被映射为镜像的文件允许改名与删除（只是不允许写入），把旧文件改名让位即可让新文件立刻就位，旧句柄继续指向改名后的文件；让位文件能删就当场删，删不掉的登记为重启后删除。逐文件覆盖**只覆盖与新增，不删除**安装目录里的其它文件（`Uninstall.exe`、日志等）。
  - **中途出错按相反顺序回滚**：已替换的文件恢复原样、新增的文件删除，不再留下半新半旧、根本起不来的安装目录；上一次失败遗留的让位文件会在下次安装开始时清掉。
- **结束旧进程改为先礼后兵**：先 `CloseMainWindow` 让主窗口与托盘自己退出（设置与历史有机会落盘），2 秒未退再整棵进程树强杀，再不行用 `taskkill` 兜底，每一步都轮询复查进程是否真的没了。同时会连**安装目录下按进程名匹配不到的可执行文件**（如 `Uninstall.exe`）一起纳入检查。
- **结束不掉不再中止安装**：把仍在运行的进程名与 PID 显示出来并写进安装日志，然后自动改用逐文件覆盖继续装。
- **安装失败提示写清下一步**：文件占用类错误会额外提示"请退出 NexClip（包括托盘图标），关闭正停留在安装目录的资源管理器窗口，或重启后重试"，并把完整异常写入安装日志与 `%TEMP%\nexclip_install_error.log`；诊断报告新增"启动时工作目录"。
- **更新通道按平台判断**：服务端直连源优先读 `version.json` 里 `windows` 段自己的版本号；GitHub 源改为在发布列表中挑选**最新一个真正带 Windows 安装包的发布**，不再直接相信 `/releases/latest`（那是两端共用的单一指针）。列表接口不可用（限流 / 网络异常）时自动退回原有路径，行为与此前一致。

### 2. Android 移动端

- **更新通道按平台判断**（与桌面端同一套规则）：服务端直连源优先读 `version.json` 里 `android` 段自己的版本号；GitHub 源改为挑选**最新一个真正挂了 APK 的发布**，跳过只发布 Windows 的版本，不再提示一个根本没有 APK 可下的"新版本"。列表接口不可用时退回原有路径。
- 本次 Android 端**无其它功能改动**，与桌面端同版号发布是为了让分平台通道在两端同时生效。`versionCode` 为 9，可正常覆盖安装。

### 3. 服务端 (Node / C#)

- 本次**无代码改动**，线上沿用 v20260904.01 的 Node 服务端。仅更新了 `releases/` 下的安装包与 `version.json`（新增各平台自己的 `version` / `tag_name` 字段，顶层字段保留不变以兼容旧客户端）。

### 4. Web 控制台

- 门户下载区**两端版本号各自独立显示**：Windows 与 Android 的版本徽标、体积、SHA256、GitHub 发布页链接分别取自 `version.json` 对应平台段，只发布单端时另一端不再跟着变。
- 下载信息更新至 v20260905.01（文件名、体积、SHA256、直连与 GitHub 双通道）。

---

## 发布文件与 SHA256 校验

| 产物文件 | 适用平台 | 文件大小 | SHA256 校验码 |
| :--- | :--- | :--- | :--- |
| **`NexClip_Setup_v20260905.01_x64.exe`** | Windows 10 (1809+) / Windows 11 (x64) | 18.92 MB | `05a6a8264c8b44f03730329a4568f17832f882ca00c1d1dc55bd6c6f30dedb3b` |
| **`NexClip_v20260905.01_Android.apk`** | Android 8.0+ (推荐 HyperOS / MIUI) | 15.26 MB | `4aa987d87dafe8786202650f56ead954cebc5814899af6bfdec5447ca32631fe` |

---

## 升级说明

- **服务端无需升级**：本次不含服务端改动，v20260904.01 的服务端可直接配合新客户端使用。
- **Windows 端强烈建议升级**：这是修掉覆盖安装占用问题的第一个版本。从 v20260904.02 或更早版本升级时，占用问题**仍可能出现在这一次**（旧桌面端启动安装器时不传工作目录）；若安装向导报"文件正由另一进程使用"，请直接双击下载好的安装包重新运行一次即可（此时安装器不再继承安装目录），或先退出 NexClip 后重试。装上本版之后，后续升级不再有这个问题。
- **Android 端建议升级**：`versionCode` 为 9，可正常覆盖安装，无需卸载。
- 分平台更新通道**要等本版装上才生效**：v20260904.02 及更早的客户端仍然只读顶层版本号，所以本次两端同版发布，避免老客户端误报。

---

## 下载通道说明

- **GitHub 官方源**：[GitHub Releases](https://github.com/yixing233/nexclip/releases/tag/v20260905.01)
- **服务端直连加速**：
  - Windows 安装包：`https://nexclip.157342.xyz/releases/NexClip_Setup_v20260905.01_x64.exe`
  - Android 安装包：`https://nexclip.157342.xyz/releases/NexClip_v20260905.01_Android.apk`
  - 检查更新配置：`https://nexclip.157342.xyz/releases/version.json`
  - Web 控制台与门户：[https://nexclip.157342.xyz](https://nexclip.157342.xyz)

---

## 已知限制

- 逐文件覆盖回退路径下，个别仍被占用的旧文件会以 `*.nexclip-old-*` 形式登记为**重启后删除**，重启前可能短暂留在安装目录内；下次安装开始时也会再清一次。
- 分平台更新通道只对本版及以后的客户端有效，老客户端仍读顶层版本号。
- 应用过滤选择器只枚举**当前正在运行**的应用；未运行的应用需手动键入进程名。
- Android 端"推送到设备"推送的是纯文本片段，不携带富文本格式。
- 富文本相关限制沿用 v20260904.01：HTML 中 `file:///` 本地图片引用跨设备无法解析，桌面端历史列表不做富文本预览渲染。

---

## 源码分支导航

- **Windows 桌面端源码**：`git clone -b windows https://github.com/yixing233/nexclip.git`
- **Android 移动端源码**：`git clone -b android https://github.com/yixing233/nexclip.git`
- **Web 端源码**：`git clone -b web https://github.com/yixing233/nexclip.git`
- **服务端源码**：`git clone -b server https://github.com/yixing233/nexclip.git`
