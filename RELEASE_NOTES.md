# NexClip v20260917.01 - Windows 剪贴板卡死修复

本次发布修复 Windows 客户端在远控软件占用剪贴板时可能被拖死、进而导致整机无法复制粘贴的问题。同时新增批量选择与诊断报告导出。

## Windows 桌面端

### 修复

- 修复远控软件（如网易 UU 远程）占用系统剪贴板时，NexClip 读取剪贴板可能被挂起的问题。此前该读取无法超时中断，会连带锁死系统剪贴板，导致全机复制粘贴失效，只能结束进程恢复。
- 剪贴板读取前新增两道本地探测：剪贴板被独占时立即跳过本轮读取，而不是发起可能挂起的跨进程请求。
- 修复剪贴板看门狗失效的问题。看门狗此前与界面线程共用调度器，界面线程一旦卡住它也随之失效；现改为独立后台线程，并会在剪贴板被长期占用时明确提示是哪个程序占用。
- 修复「打开复制的网址」快捷键在读取剪贴板时阻塞界面线程的问题。
- 修复批量选择工具栏图标缺失导致构建失败的问题。

### 新增

- 新增批量选择模式。在列表顶栏进入后，可多选记录并批量收藏、取消收藏或删除。
- 设置页新增「导出诊断报告」，可一键导出监听状态、连接状态与近期日志，便于排查问题。

### 优化

- 内置剪贴板来源过滤列表补充网易 UU 远程等远控程序；来源过滤提前到读取剪贴板之前，命中即跳过，不再向其索取数据。
- 剪贴板被其它程序短暂占用时会自动退避重试，持续占用则暂停读取并在托盘提示占用方。

## 发布文件

| 产物文件 | 适用平台 | 文件大小 | SHA256 校验码 |
| :--- | :--- | :--- | :--- |
| `NexClip_Setup_v20260917.01_x64.exe` | Windows 10 (1809+) / Windows 11 (x64) | 19.01 MB | `11e704388e93f9c96544aad328d71bd4fd2edc4c08e63ecd6a89ef76e3d1886e` |

## 升级说明

- 本次仅发布 Windows 客户端，Android、服务端与 Web 控制台无需升级。
- 可直接覆盖安装，现有配置、历史记录和收藏状态会继续保留。
- 若升级前已出现复制粘贴失效，安装后如仍存在，请先退出正在运行的远控软件再试。
- 下载地址：`https://github.com/yixing233/nexclip/releases/tag/v20260917.01`
