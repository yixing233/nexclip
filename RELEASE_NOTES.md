# NexClip v20260918.01 - 批量粘贴与收藏同步

本次发布为 Windows 客户端新增批量粘贴，并让收藏与备注状态在服务端保持一致。同时修复搜索框重复出现清除按钮、以及在系统内存在无响应窗口时客户端被拖住的问题。

## Windows 桌面端

### 新增

- 新增批量粘贴。在批量选择模式下多选记录后点击「粘贴」，所选文本会按记录时间由旧到新合并为一段，一次性粘贴到呼出 NexClip 之前的光标位置。
- 多选内容中包含图片或文件时，这些条目会被跳过，并在托盘提示实际合并的条数与跳过的条数。

### 优化

- 单条收藏与备注现在会同步到服务端。此前收藏只写入本机，服务端并不知情；现在收藏状态在同一用户组的设备间保持一致，服务端的自动清理也不会再删除被收藏的记录及其图片。

### 修复

- 修复批量选择工具栏浮在列表上方、遮挡第一条记录的问题。工具栏现在独立占一行，不再压住记录内容。
- 修复搜索框右侧同时出现两个清除按钮的问题。此前隐藏系统自带清除按钮的写法未能生效，两个按钮一直叠加显示。
- 修复在系统内存在无响应窗口（例如卡住的第三方程序）时，客户端启动或呼出后可能长时间无响应、托盘图标与快捷键一并失效的问题。焦点解析相关的系统调用已改为后台执行，并加入超时放弃与日志记录。

### 优化（续）

- 批量选择工具栏的收藏、取消收藏、删除改为带文字的图标按钮，未选中任何记录时按钮呈禁用状态，避免点了没有反应。

## 服务端

- 新增收藏与备注字段，并新增单条更新与批量操作接口，供各端同步收藏状态。
- 历史上限清理现在只统计未收藏记录。被收藏的记录不再参与自动淘汰，与桌面端本地清理规则保持一致。

## 发布文件

| 产物文件 | 适用平台 | 文件大小 | SHA256 校验码 |
| :--- | :--- | :--- | :--- |
| `NexClip_Setup_v20260918.01_x64.exe` | Windows 10 (1809+) / Windows 11 (x64) | 19.01 MB | `8a47be007710f090f3b81acf87014fa2e1c464b6149716f04ef8940a95e0e7eb` |

## 升级说明

- 本次发布包含 Windows 客户端与服务端，服务端需同步部署；Android 与 Web 控制台无需升级。
- 可直接覆盖安装，现有配置、历史记录和收藏状态会继续保留。
- 下载地址：`https://github.com/yixing233/nexclip/releases/tag/v20260918.01`
