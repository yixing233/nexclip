# NexClip v20260904.02 - 输入法候选栏归位与 Android 应用内更新

NexClip 是一套专为多端设备打造的现代化、轻量高效跨平台剪贴板同步与文本 / 图片消息互传系统。本次 **v20260904.02** 是紧随 v20260904.01 的体验修复版：Windows 端解决了呼出剪贴板后**中文输入法候选栏跳到屏幕左上角**这一长期存在的干扰，并把应用过滤选择器重做成可搜索、带图标的列表；Android 端让**应用内更新真正可用**（此前会卡在唤起安装器），新增记录详情的文本选中菜单，并修复了记录页触底分页失效。服务端本次无改动。

---

## 本次更新核心亮点 (Changelog)

### 1. Windows 桌面端

- **输入法候选栏不再跳到屏幕左上角**：呼出 / 收起剪贴板窗口后回到原输入框继续打字，中文候选栏保持跟随插入符。两处根因一并修掉：
  - 查询目标窗口焦点控件时不再 `AttachThreadInput`。`GetGUIThreadInfo` 本身就支持跨进程查询（需要共享输入队列的是 `GetFocus` / `GetCaretPos`），而合并输入队列会连带搅乱目标线程的输入法上下文，detach 之后输入法查不到插入符矩形。
  - 焦点已经自然回到原输入框时，不再多余地强设一次焦点。跨进程 `SetFocus` 与 UIA `SetFocus` 只改 Win32 焦点，目标应用不会重走自己的获焦握手，TSF 焦点文档因此与 Win32 焦点脱节。
- **回焦策略更精细**：新增焦点完整性判定——Chromium / Electron 比对 UIA 焦点元素（Win32 焦点停在渲染窗口上不代表 DOM 输入框已回焦），普通应用比对焦点控件句柄；判定刻意保守，拿不准一律退回强制回焦，**不牺牲粘贴成功率**。UIA `SetFocus` 在普通 TSF 应用上降级为 Win32 回焦失败后的最后一招。
- **收起窗口不阻塞 UI 线程**：热键 / 托盘隐藏路径改为异步等待前台自然恢复。
- **应用过滤选择器重做**：原来的下拉框换成可搜索输入框，支持按应用名 / 进程名 / 可执行文件路径筛选，聚焦即展开完整列表；候选项**不再截断数量**（弹出层内滚动 + 列表虚拟化 + 图标懒解码），每项带 16px 应用图标。
- **可添加当前未运行的应用**：直接键入进程名即可加入过滤列表（自动去掉首尾引号与 `.exe` 后缀）；添加后状态行给出明确回执（已添加 / 已在列表中），不再只显示"没有匹配…"让人以为没成功。
- **对话框交互补齐**：设置内的对话框支持**点击遮罩关闭**；点搜索框外侧即失焦并收起建议列表（拦掉框架收起列表时的程序化回焦，消除"失焦后立刻又自动聚焦"）。

### 2. Android 移动端

- **应用内更新真正可用**：启动检查发现新版本后，通知条动作由"查看"（跳浏览器）改为"更新"，直接在应用内下载并唤起安装。
  - 补齐 `REQUEST_INSTALL_PACKAGES` 权限声明与 FileProvider 的 `updates/` 目录（外部存储 + 内部存储两条），此前拿不到安装包的 `content://` URI。
  - 安装前检查"安装未知应用"授权，未授权时直接跳授权页并给出可读提示，返回应用点"已授权，安装"即可继续；并对解析到的系统安装器包名显式授权（部分 ROM 不认 Intent 上的临时授权）。
  - 下载落盘更稳：跨分区 `renameTo` 失败时回退为复制 + 删除，并校验最终文件非空。
  - 下载失败不再一闪而过：进度区常驻显示失败原因，按钮变为"重试下载"。
- **记录详情新增文本选中菜单**：选中正文片段后，浮动条上常驻"推送到设备"，可把**选中片段**（而非整条记录）单独推给其他设备，另附"搜索"与"分享"（系统已提供分享时不重复添加）。正文改用原生文本控件承载，因为只有原生 `ActionMode` 能把自定义项排到剪切 / 复制之前并强制显示在浮动条上。
- **修复记录页触底分页失效**：滚动到底部不再卡在"正在加载更多"。原因是分页判定把列表闭包进了只创建一次的派生状态里，永久持有首次组合时的列表快照。
- **修复内存记录数与磁盘不一致**：落盘函数现在返回真正保存下来的列表（已按记录上限裁剪、去重、按时间倒序）并回写内存状态。此前上限 100 时内存会停在 101 条，重启后突然变短，还会让触底分页永远差最后一条。
- **修复检查更新的版本号比较**：此前传入的版本串带 `versionCode` 后缀（形如 `20260904.01 (7)`），按 `.` 切分后第二段不是纯数字，会落到字符串比较分支，序号跨位数时误判；现在只传 `versionName`。
- **设置页去重**：移除"项目信息"分组里重复的"启动检查新版本"开关与"更新下载来源"下拉（同一对控件在"基础与界面"分组已有）；移除多余的尾部占位，不再重复计一份底部避让高度。

### 3. 服务端 (Node / C#)

- 本次**无代码改动**，线上沿用 v20260904.01 的 Node 服务端（`Entries.Html` 富文本字段已在线生效）。仅更新了 `releases/` 下的安装包与 `version.json`。

### 4. Web 控制台

- 门户下载信息更新至 v20260904.02（文件名、体积、SHA256、直连与 GitHub 双通道）。

---

## 发布文件与 SHA256 校验

| 产物文件 | 适用平台 | 文件大小 | SHA256 校验码 |
| :--- | :--- | :--- | :--- |
| **`NexClip_Setup_v20260904.02_x64.exe`** | Windows 10 (1809+) / Windows 11 (x64) | 18.86 MB | `2c76e906ac7991c2bda8c77336d4edb147791d46eb9bdd3bb3d00c6286f1331a` |
| **`NexClip_v20260904.02_Android.apk`** | Android 8.0+ (推荐 HyperOS / MIUI) | 15.26 MB | `994bfb31d3aa97958dfdc41e584761df4e201cd3da97f65308616d588c30a9c3` |

---

## 升级说明

- **服务端无需升级**：本次不含服务端改动，v20260904.01 的服务端可直接配合新客户端使用。
- **Windows 端建议升级**：输入法候选栏错位的修复只在客户端侧。
- **Android 端建议升级**：应用内更新链路是本次修复的，从更早版本升级时若应用内安装仍失败，可先在系统设置中允许 NexClip"安装未知应用"，或直接下载 APK 手动覆盖安装。
- 本次 Android `versionCode` 为 8，可正常覆盖安装，无需卸载。

---

## 下载通道说明

- **GitHub 官方源**：[GitHub Releases](https://github.com/yixing233/nexclip/releases/tag/v20260904.02)
- **服务端直连加速**：
  - Windows 安装包：`https://nexclip.157342.xyz/releases/NexClip_Setup_v20260904.02_x64.exe`
  - Android 安装包：`https://nexclip.157342.xyz/releases/NexClip_v20260904.02_Android.apk`
  - 检查更新配置：`https://nexclip.157342.xyz/releases/version.json`
  - Web 控制台与门户：[https://nexclip.157342.xyz](https://nexclip.157342.xyz)

---

## 已知限制

- 应用过滤选择器只枚举**当前正在运行**的应用；未运行的应用需手动键入进程名。
- Android 端"推送到设备"推送的是纯文本片段，不携带富文本格式。
- 富文本相关限制沿用 v20260904.01：HTML 中 `file:///` 本地图片引用跨设备无法解析，桌面端历史列表不做富文本预览渲染。

---

## 源码分支导航

- **Windows 桌面端源码**：`git clone -b windows https://github.com/yixing233/nexclip.git`
- **Android 移动端源码**：`git clone -b android https://github.com/yixing233/nexclip.git`
- **Web 端源码**：`git clone -b web https://github.com/yixing233/nexclip.git`
- **服务端源码**：`git clone -b server https://github.com/yixing233/nexclip.git`
