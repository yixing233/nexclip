# NexClip v20260904.01 - 全链路富文本(HTML)剪贴板同步

NexClip 是一套专为多端设备打造的现代化、轻量高效跨平台剪贴板同步与文本 / 图片消息互传系统。本次 **v20260904.01** 的核心是**富文本(HTML)剪贴板能力**：从 Word / Excel / 浏览器复制的带格式内容不再被降级成纯文本，而是原样保留、原样粘贴，并可跨设备同步；同时激活了此前一直存在但无处生效的"粘贴为纯文本"交互，并补入窗口置顶热键与一轮内存占用优化。

---

## 本次更新核心亮点 (Changelog)

### 1. Windows 桌面端

- **富文本(HTML)捕获与写回**：剪贴板中的 HTML 片段与纯文本一并捕获入库；粘贴时同时写入 HTML 与纯文本两种格式，任何目标程序都至少能拿到纯文本，支持富文本的程序则完整保留字体、颜色、链接与表格结构。
- **"粘贴为纯文本"正式生效**：右键菜单"粘贴为纯文本"、`Shift+回车`、`Shift+双击` 三条入口此前对文本条目无实际差异，现在可在"保留格式"与"只要文字"之间精确切换。
- **富文本类型角标**：历史列表为带格式条目显示独立的 Lucide 矢量角标，与纯文本、图片条目一眼可分。
- **位图 / 富文本智能判定**：从 Excel、Word 复制的表格（同时带位图 + HTML + 纯文本）识别为富文本条目而非图片；纯截图、浏览器复制图片仍按图片条目处理，不会误判。
- **富文本开关**：设置页新增"富文本(HTML)"开关（默认开启），关闭后捕获与写回均完全回退到纯文本行为。单条 HTML 上限 256 KB，超限自动只保留纯文本。
- **存量数据零影响**：历史库自动幂等补列，旧条目的内容指纹保持逐字节不变，复制旧内容仍是"置顶"而不是新增重复项。
- **窗口置顶热键**：新增 `Ctrl+Alt+T` 切换剪贴板窗口置顶，标题栏按钮的提示文本跟随实际注册成功的热键动态显示。
- **内存与渲染优化**：隐藏到托盘 90 秒后主动回收工作集；历史列表与传输会话在运行时限制驻留条数；缩略图在设置图源前先声明解码尺寸，不再按原图全尺寸解码。
- **直达卡片不再抢焦点**：复制后的右下角直达卡片以非激活方式弹出，不会再把焦点从剪贴板窗口抢走；关闭时同步解绑倒计时。
- **托盘重启握手**：托盘菜单触发的重启改为 `--restart-wait` 等待式握手，避免新旧进程同时退出导致"重启后没起来"。

### 2. Android 移动端

- 本次为**版本对齐发布**，功能与 v20260901.01 一致（通知栏 / 超级岛图片复制、来源设备精准解析、输入法焦点优化）。
- 与桌面端富文本**向后兼容**：收到带 HTML 的条目时忽略该字段，按纯文本正常显示与复制，无需升级即可共存。

### 3. 服务端 (Node / C#)

- **富文本字段透传**：`Entries` 表新增 `Html` 列，上传、查询、实时推送与 DTO 全链路贯通；两套服务端列名与类型完全一致，可互换使用。
- **旧库自动补列**：Node 与 C# 服务端均在启动时做幂等 `ALTER TABLE` 检测补列（C# 端此前只有 `EnsureCreated`、无迁移，旧库升级后会直接报缺列，本次一并修复）。
- **上传体积上限调整**：`PUT /api/clipboard` 请求体上限提升到 2 MB，容纳纯文本 + HTML 的组合；单条 HTML 超过 256 KB 时静默丢弃 HTML、保留纯文本。
- **实时广播按用户组定向**：推送不再面向全部连接广播，而是限定在同一用户的设备组内。
- **修复可信用户直插失败**：`Users.Name` 为 `NOT NULL UNIQUE`，此前受信任用户首次落库时未写入 `Name`，必然触发约束错误，现与 `createUser` 约定一致（默认取 `Id`）。

### 4. Web 控制台

- 门户下载信息更新至 v20260904.01（文件名、体积、SHA256、直连与 GitHub 双通道）。
- 富文本条目在 Web 端**仍按纯文本渲染**：不注入、不执行远端 HTML，这是有意为之的安全取舍。

---

## 发布文件与 SHA256 校验

| 产物文件 | 适用平台 | 文件大小 | SHA256 校验码 |
| :--- | :--- | :--- | :--- |
| **`NexClip_Setup_v20260904.01_x64.exe`** | Windows 10 (1809+) / Windows 11 (x64) | 18.85 MB | `412a08466a40514320cf229f11a8762f2e5334ba7ae9057024d760e4c09dc9ec` |
| **`NexClip_v20260904.01_Android.apk`** | Android 8.0+ (推荐 HyperOS / MIUI) | 15.29 MB | `60c8822b698caaa7a0682ad8ddd607faf123f637b107a11d5ebb022c3b869964` |

---

## 升级说明

- **建议桌面端与服务端一起升级**：富文本字段需要服务端支持才能跨设备保留格式；若只升级桌面端，本机富文本可用，跨设备同步会退化为纯文本。
- 服务端首次启动新版本会自动补列，无需手工执行 SQL，也不会影响既有数据。
- Android 端可不升级，与新版桌面端 / 服务端共存无副作用。

---

## 下载通道说明

- **GitHub 官方源**：[GitHub Releases](https://github.com/yixing233/nexclip/releases/tag/v20260904.01)
- **服务端直连加速**：
  - Windows 安装包：`https://nexclip.157342.xyz/releases/NexClip_Setup_v20260904.01_x64.exe`
  - Android 安装包：`https://nexclip.157342.xyz/releases/NexClip_v20260904.01_Android.apk`
  - 检查更新配置：`https://nexclip.157342.xyz/releases/version.json`
  - Web 控制台与门户：[https://nexclip.157342.xyz](https://nexclip.157342.xyz)

---

## 已知限制

- 富文本中引用的 `file:///` 本地图片跨设备无法解析（`https://` 图片正常）。
- 桌面端历史列表不做富文本预览渲染，仍显示纯文本摘要 + 类型角标。
- 暂不支持 RTF 载体（HTML 已覆盖 Word / WPS / Office / 浏览器场景，且是唯一能跨到移动端与 Web 的格式）。

---

## 源码分支导航

- **Windows 桌面端源码**：`git clone -b windows https://github.com/yixing233/nexclip.git`
- **Android 移动端源码**：`git clone -b android https://github.com/yixing233/nexclip.git`
- **Web 端源码**：`git clone -b web https://github.com/yixing233/nexclip.git`
- **服务端源码**：`git clone -b server https://github.com/yixing233/nexclip.git`
